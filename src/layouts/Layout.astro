---
interface Props {
	title?: string;
	description?: string;
}

const {
	title = 'Nick | UX Designer & Writer',
	description = 'Personal website with UX portfolio work and blog posts.',
} = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<meta name="description" content={description} />
		<title>{title}</title>
	</head>
	<body>
		<header class="site-header">
			<div class="wrap">
				<a class="brand" href="/">Nick</a>
				<nav aria-label="Main navigation">
					<a href="/work">Work</a>
					<a href="/blog">Blog</a>
					<a href="/about">About</a>
				</nav>
			</div>
		</header>
		<main class="wrap">
			<slot />
		</main>
		<footer class="site-footer wrap">
			<p>© {new Date().getFullYear()} Nick. UX freelance practice and personal writing.</p>
		</footer>
	</body>
</html>

<style>
	:root {
		--bg: #f4f0e8;
		--surface: #fffdf8;
		--text: #2a221b;
		--muted: #6f6458;
		--accent: #b45a31;
		--line: #d8cfc2;
	}

	html,
	body {
		margin: 0;
		padding: 0;
		font-family: 'Avenir Next', 'Gill Sans', 'Trebuchet MS', sans-serif;
		background:
			radial-gradient(circle at 10% 10%, #f8f5ee 0, transparent 35%),
			radial-gradient(circle at 90% 20%, #efe7da 0, transparent 30%),
			var(--bg);
		color: var(--text);
	}

	a {
		color: inherit;
	}

	.wrap {
		width: min(980px, calc(100% - 2rem));
		margin: 0 auto;
	}

	.site-header {
		border-bottom: 1px solid var(--line);
		backdrop-filter: blur(2px);
	}

	.site-header .wrap {
		display: flex;
		align-items: center;
		justify-content: space-between;
		padding: 1rem 0;
	}

	.brand {
		font-size: 1.2rem;
		font-weight: 800;
		letter-spacing: 0.08em;
		text-decoration: none;
	}

	nav {
		display: flex;
		gap: 1rem;
	}

	nav a {
		text-decoration: none;
		font-weight: 600;
		color: var(--muted);
	}

	nav a:hover {
		color: var(--accent);
	}

	main {
		padding: 2.25rem 0 2.5rem;
	}

	.site-footer {
		border-top: 1px solid var(--line);
		padding: 1.25rem 0 2rem;
		color: var(--muted);
		font-size: 0.9rem;
	}

	@media (max-width: 720px) {
		nav {
			gap: 0.75rem;
			font-size: 0.95rem;
		}
	}
</style>
